{
  "name": "handoffpoc",
  "version": "0.0.1",
  "private": true,
  "description": "Email subscription module (backend + frontend) for the handoff skill POC.",
  "scripts": {
    "server:dev": "ts-node src/server/index.ts",
    "server:build": "tsc -p tsconfig.json",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.12",
    "@types/node": "^22.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.8.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": ["**/src/server/**/*.test.ts"]
  }
}
